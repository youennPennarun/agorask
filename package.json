{
  "name": "agorask",
  "version": "0.0.1",
  "scripts": {
    "install": "cd ./server && npm install && cd ../mobile_app && npm install",
    "test": "cd ./server && npm test && cd ../mobile_app && npm test"
  }
}
